{
  "name": "boj-class-to-main",
  "displayName": "BOJ Helper",
  "description": "Automatically convert class name to Main and copy code for BOJ submissions, and create BOJ Java files from URLs.",
  "version": "0.0.2",
  "publisher": "minus43",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:bojhelper.copyCode",
    "onCommand:bojhelper.createFile"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "bojhelper.copyCode",
        "title": "Copy BOJ Code",
        "icon": {
          "light": "images/copy-light.svg",
          "dark": "images/copy-dark.svg"
        }
      },
      {
        "command": "bojhelper.createFile",
        "title": "Create BOJ Java File",
        "icon": {
          "light": "images/create-light.svg",
          "dark": "images/create-dark.svg"
        }
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "bojhelper.copyCode",
          "when": "resourceExtname == .java",
          "group": "navigation"
        },
        {
          "command": "bojhelper.createFile",
          "when": "true",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/vscode": "^1.90.0",
    "@types/node": "latest",
    "typescript": "^5.6.0"
  },
  "license": "MIT",
  "icon": "icon.png"
}
